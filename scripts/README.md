# Scripts

This folder contains small public demonstration scripts.

Current files:

- `01_synthetic_component_mapping.py`: creates a synthetic regional component, summarises mock annotation evidence and writes a simple figure.

Scripts in this repository should run without controlled-access data.
